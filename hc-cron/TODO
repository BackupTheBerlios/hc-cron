- I created the preliminary configure.in script automatically.
  Do we need all these tests in configure.in? Do we even use all of them?

- merge Steve's changes from the debian version of vixie-cron (LoLo)

- /proc/stat changed format in linux 2.3.99pre8 and 2.4.0test9, adapted to
2.4.x format. We should be able to detect which format is used at compile time
